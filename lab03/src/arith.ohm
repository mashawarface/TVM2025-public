Arithmetic {
    Sum = Mul (add Mul)*
    add = "+" | "-"

    Mul = Neg (mul Neg)*
    mul = "*" | "/"

    Neg = "-" Neg   --neg
        | Atom

    Atom = number | variable | Paren 
    Paren = "(" Sum ")"
    variable = letter+
    number = digit+
}